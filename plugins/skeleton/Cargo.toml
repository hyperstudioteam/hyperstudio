[package]
name = "hypergrid-skeleton"
version = "0.1.0"
edition = "2021"
description = "Minimal Hypergrid driver plugin skeleton (JSON-RPC over stdin/stdout)"
license = "MIT"
publish = false

[[bin]]
name = "hypergrid-skeleton"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
